<div>
    {{ message|raw }}
    <a href="{{ url({params: {
        extra_activity_mode: mode,
        course: course,
        module: module,
        tag:tag,
        learner: learner,
        confirm:'yes',
        debug: debug ? 'yes':null} }) }}"
      class="btn btn-danger btn-sm">
      {{ _t('YES') }}
    </a>
    <a href="{{ url({params: {
        course: course,
        module: module,
        learner: learner,
        debug: debug ? 'yes':null} }) }}"
      class="btn btn-primary btn-sm" >
      {{ _t('NO') }}
    </a>
</div>